## Objective

1. To split post-ingestion folder into 3-pane window; top pane (10% of space) shows number of files to process, bottom pane(90%) is split into two (50/50)
2. Bottom left pane is a scrollable pane 
3. Bottom left pane contains all ingested file names

## Addressing 1 

- This likely requires a bit more digging into how to set the exact spacing/sizing parameters; though, it does beg the question of whether or not 
people actually use some sort of templating system with a markup language, like HTML/CSS, in order to describe layouts
  - Save that last thought for a bit later 
  - For now, just rely on source code. Take a look at Box and how it handles "panes".
- First, start by splitting into two panes. Place a label in the bottom pane to distinguish it from the top pane.
- Easy enough, just needs some work on the composition 

## Addressing 2 

- Done essentially by going through steps in 1 

## Addressing 3 

- Passing the path into the builder function, we just need to loop through all files in there. Take a look at the counting method, that has basically the same logic.
  - We just need to build a label for each entry. Eventually, this should probably be a custom widget, or one that can be selected more easily.
  - Maybe try out GtkEntry?
  - Nah, why not just use a Label? You are overthinking this, as you can format it any way you want. You can even use HTML-like markup.
